{
  "name": "knowyourresume",
  "version": "1.0.0",
  "private": true,
  "description": "A privacy-first resume builder with deterministic ATS analysis and optional Gemini assistance.",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test tests/ats-engine.test.js tests/server-ai.test.js tests/auth.test.js",
    "lint": "node --check server.js && node --check lib/ats-engine.js && node --check public/app.js && node --check public/lib/firebase-auth.js"
  },
  "engines": {
    "node": ">=20"
  }
}
